import { VoiceInterface } from "@/components/VoiceInterface";

const Index = () => {
  return (
    <div className="dark min-h-screen bg-gradient-to-br from-background via-background to-muted">
      <VoiceInterface />
    </div>
  );
};

export default Index;
